package es.ull.project.domain.enumerate;

/**
 * @brief Enum representing different icons that can be associated with a node.
 */
public enum NodeIcon {

    ROBOT,
    FILE,
    WARNING,
    GEAR,
    FOLDER,
    START;
}
